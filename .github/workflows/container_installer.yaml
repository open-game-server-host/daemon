name: build

on:
  push:
    branches:
    - '*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Docker login
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - name: Set up Docker
      uses: docker/setup-buildx-action@v3
    - name: Build and Publish Docker image
      uses: docker/build-push-action@v6
      with:
        push: true
        tags: ghcr.io/open-game-server-host/container-installer:${{ github.ref_name }}
        context: container_installer
